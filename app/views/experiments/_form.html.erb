        
        <% if @experiment.errors.any? %>
            <div id="error_explanation" class="alert alert-block">
                <h4><%= pluralize(@experiment.errors.count, "error") %> prohibited this dashboard from being saved:</h4>
                <ul>
                    <% @experiment.errors.each do |msg| %>
                        <li><%= msg %></li>
                    <% end %>
                </ul>
            </div>
        <% end %>

            <div class="control-group">
                <label class="control-label" for="inputEmail">Name</label>
                <div class="controls">
                    <%= text_field_tag "name", @experiment.name, :placeholder => "Humanized name" %>
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputEmail">Url</label>
                <div class="controls">
                    <%= text_field_tag "url", @experiment.url, :placeholder => "Url of experiment page" %>
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputEmail">Url</label>
                <div class="controls">
                    <%= select_tag "type", options_for_select(['Local', 'Global'], @experiment.type) %>
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputEmail">Description</label>
                <div class="controls">
                    <%= text_area_tag "description", @experiment.description, :placeholder => "What does this experiment test?", :rows=>"3" %>
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputEmail">Screenshot (optional)</label>
                <div class="controls">
                    <%= text_field_tag "screenshot", @experiment.screenshot_url, :placeholder => "URL" %>
                </div>
            </div>
<%@experiment.global_tracked_kpis = ['ftb', 'ftg']%>
<%@experiment.experiment_tracked_kpis = ['ftb', 'ftg']%>
            <div class="control-group">
                <label class="control-label" for="inputEmail">Tracked KPIs</label>
                <div class="controls">
              		<ul>
									  <% @experiment.global_tracked_kpis.each do |kpi| %>
									      <li>
									        <%= check_box_tag 'global_tracked_kpis[]', kpi -%>
									        <%= kpi -%>
									      </li>
									  <% end %>
									</ul>
                </div>
            </div>

            <div class="actions">
                <%= submit_tag "Continue", :class=>'btn btn-success btn-large' %>
            </div>
